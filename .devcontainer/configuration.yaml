# Loads default set of integrations. Do not remove.
default_config:

logger:
  default: warning
  logs:
    custom_components.smart_energy_goecharger: debug
    custom_components.hacs: debug
    aiogithubapi: debug

sensor:
  - platform: time_date
    display_options:
      - 'date_time'

smart_energy_goecharger:
  chargers:
    - name: charger1
      host: http://localhost:4000
      api_token: TEST
